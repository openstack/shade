- project:
    templates:
      - openstack-python-jobs
      - release-notes-jobs
      - check-requirements
      - openstack-python35-jobs
    check:
      jobs:
        - openstack-tox-cover:
            voting: false
    post:
      jobs:
        - openstack-tox-cover
